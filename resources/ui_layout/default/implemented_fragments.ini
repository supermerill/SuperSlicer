; Plugin UI fragments already implemented directly by this layout.
; Sections are logical layout names. Enabled keys are fragment ids that must
; be skipped by the plugin UI merger to avoid injecting duplicate controls.

[print]
step_layer_height_plugin = 1
step_perimeter_plugin = 1
